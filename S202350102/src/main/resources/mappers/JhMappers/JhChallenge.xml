<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.oracle.S202350102.JhChallengMapper">

	<select id="JhChgDetail" parameterType="int" resultType="Challenge">
		SELECT *
		FROM challenge
		WHERE chg_id = #{chg_id}
	</select>
	
	<select id="JhChgCtn" parameterType="Challenge" resultType="java.lang.String">
		SELECT c.ctn
		FROM   comm c 
		INNER JOIN challenge ch
		        ON c.md = ch.chg_md
		WHERE  c.md = #{chg_md}
		AND    c.lg = 200
		AND    ch.chg_id = #{chg_id}
	</select>
	
	<select id="jhChgCreator" parameterType="Challenge" resultType="java.lang.String">
		SELECT u.nick
		FROM   user1 u 
		INNER JOIN challenge ch
		        ON u.user_num = ch.user_num
		WHERE  u.user_num = #{user_num}
		AND    ch.chg_id = #{chg_id}
	</select>
	
</mapper> 